document.addEventListener("DOMContentLoaded", () => {
  const app = document.getElementById("app");
  app.innerHTML = "<h1>Knick Knack.<br><small>Paddywack.</small></h1>";
  console.log("Findthinger initialized");
});

// This is absolutely the smallest change I could possibly think of making to this file, just to make sure the local to GitHub connection is working. Really.
